eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign

#  feign已经集成了hystrix，需要手动开启
feign:
  hystrix:
    enabled: true